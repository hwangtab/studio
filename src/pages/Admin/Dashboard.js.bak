import React, { useState, useEffect } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { motion } from 'framer-motion';
import { FaPlus, FaEdit, FaTrash, FaSignOutAlt } from 'react-icons/fa';
import { getAllStories, deleteStory, isAdminLoggedIn, logoutAdmin } from '../../utils/localDataUtils';

const Dashboard = () => {
  const navigate = useNavigate();
  const [stories, setStories] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);
  const [deleteConfirm, setDeleteConfirm] = useState(null);
  const [deleteSuccess, setDeleteSuccess] = useState(false);

  // uad00ub9acuc790 ub85cuadf8uc778 ud655uc778
  useEffect(() => {
    if (!isAdminLoggedIn()) {
      navigate('/admin/login');
    }
  }, [navigate]);

  // uc2a4ud1a0ub9ac ub370uc774ud130 ubd88ub7ecuc624uae30
  useEffect(() => {
    const fetchStories = async () => {
      try {
        setLoading(true);
        const storiesData = await getAllStories();
        setStories(storiesData);
        setLoading(false);
      } catch (error) {
        console.error('uc2a4ud1a0ub9ac ubd88ub7ecuc624uae30 uc624ub958:', error);
        setError('uc2a4ud1a0ub9acub97c ubd88ub7ecuc624ub294 uc911 uc624ub958uac00 ubc1cuc0ddud588uc2b5ub2c8ub2e4.');
        setLoading(false);
      }
    };

    fetchStories();
  }, [deleteSuccess]);

  // ub85cuadf8uc544uc6c3 ucc98ub9ac
  const handleLogout = () => {
    logoutAdmin();
    navigate('/admin/login');
  };

  // uc2a4ud1a0ub9ac uc0aduc81c ud655uc778
  const confirmDelete = (id) => {
    setDeleteConfirm(id);
  };

  // uc2a4ud1a0ub9ac uc0aduc81c ucde8uc18c
  const cancelDelete = () => {
    setDeleteConfirm(null);
  };

  // uc2a4ud1a0ub9ac uc0aduc81c ucc98ub9ac
  const handleDelete = async (id) => {
    try {
      await deleteStory(id);
      setDeleteConfirm(null);
      setDeleteSuccess(true);
      
      // 3ucd08 ud6c4 uc131uacf5 uba54uc2dcuc9c0 uc228uae30uae30
      setTimeout(() => {
        setDeleteSuccess(false);
      }, 3000);
    } catch (error) {
      console.error('uc2a4ud1a0ub9ac uc0aduc81c uc624ub958:', error);
      setError('uc2a4ud1a0ub9acub97c uc0aduc81cud558ub294 uc911 uc624ub958uac00 ubc1cuc0ddud588uc2b5ub2c8ub2e4.');
    }
  };

  // ub85cub529 uc911 ud45cuc2dc
  if (loading) {
    return (
      <div className="container mx-auto px-4 py-8">
        <h1 className="text-3xl font-bold mb-8">ub300uc2dcubcf4ub4dc ud398uc774uc9c0uc758</h1>
        <div className="flex justify-center items-center h-64">
          <p className="text-lg">uc2a4ud1a0ub9acub97c ubd88ub7ecuc624ub294 uc911...</p>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gray-100 dark:bg-gray-900 p-4">
      <div className="max-w-6xl mx-auto bg-white dark:bg-gray-800 rounded-xl shadow-lg p-6">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5 }}
        >
          <div className="flex justify-between items-center mb-8">
            <h1 className="text-3xl font-bold text-gray-800 dark:text-white">ub300uc2dcubcf4ub4dc ud398uc774uc9c0uc758</h1>
            
            <div className="flex items-center space-x-4">
              <Link
                to="/admin/story/new"
                className="flex items-center px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary-dark transition-colors duration-300"
              >
                <FaPlus className="mr-2" />
                uc2a4ud1a0ub9ac uc0aduc81c
              </Link>
              
              <button
                onClick={handleLogout}
                className="flex items-center px-4 py-2 bg-gray-200 dark:bg-gray-700 text-gray-700 dark:text-gray-300 rounded-lg hover:bg-gray-300 dark:hover:bg-gray-600 transition-colors duration-300"
              >
                <FaSignOutAlt className="mr-2" />
                ub85cuadf8uc544uc6c3
              </button>
            </div>
          </div>
          
          {error && (
            <div className="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg mb-6">
              {error}
            </div>
          )}
          
          {deleteSuccess && (
            <div className="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg mb-6">
              uc2a4ud1a0ub9ac uc0aduc81cuc774 uc911 uc624ub958uac00 ubc1cuc0ddud588uc2b5ub2c8ub2e4.
            </div>
          )}
          
          {stories.length === 0 ? (
            <div className="text-center py-12">
              <p className="text-gray-500 dark:text-gray-400 text-lg">uc2a4ud1a0ub9ac uc0aduc81cuc5d0 uc8fcuc138uc694.</p>
              <Link
                to="/admin/story/new"
                className="inline-block mt-4 px-6 py-2 bg-primary text-white rounded-lg hover:bg-primary-dark transition-colors duration-300"
              >
                uc2a4ud1a0ub9ac uc0aduc81c uc0aduc81cud558uc5ec uc2dcubcf4ub4dc
              </Link>
            </div>
          ) : (
            <div className="overflow-x-auto">
              <table className="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
                <thead className="bg-gray-50 dark:bg-gray-700">
                  <tr>
                    <th scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-300 uppercase tracking-wider">
                      uc2a4ud1a0ub9ac uc0aduc81c
                    </th>
                    <th scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-300 uppercase tracking-wider">
                      uc2a4ud1a0ub9ac uc0aduc81c
                    </th>
                    <th scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-300 uppercase tracking-wider">
                      uc0aduc81cud558uc5ec uc2dcubcf4ub4dc
                    </th>
                    <th scope="col" className="px-6 py-3 text-right text-xs font-medium text-gray-500 dark:text-gray-300 uppercase tracking-wider">
                      uc2a4ud1a0ub9ac uc0aduc81c
                    </th>
                  </tr>
                </thead>
                <tbody className="bg-white dark:bg-gray-800 divide-y divide-gray-200 dark:divide-gray-700">
                  {stories.map((story) => (
                    <tr key={story.id} className="hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors duration-150">
                      <td className="px-6 py-4 whitespace-nowrap">
                        <div className="text-sm font-medium text-gray-900 dark:text-white">{story.title}</div>
                      </td>
                      <td className="px-6 py-4 whitespace-nowrap">
                        <span className="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800 dark:bg-blue-800 dark:text-blue-100">
                          {story.category === 'work' ? 'uc2a4ud1a0ub9ac' : 
                           story.category === 'interview' ? 'uc2a4ud1a0ub9ac' : 
                           story.category === 'tips' ? 'uc2a4ud1a0ub9ac' : story.category}
                        </span>
                      </td>
                      <td className="px-6 py-4 whitespace-nowrap">
                        <div className="text-sm text-gray-500 dark:text-gray-400">
                          {new Date(story.date).toLocaleDateString('ko-KR')}
                        </div>
                      </td>
                      <td className="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                        {deleteConfirm === story.id ? (
                          <div className="flex items-center justify-end space-x-2">
                            <span className="text-gray-700 dark:text-gray-300">uc2a4ud1a0ub9ac uc0aduc81cuc774 uc911 uc624ub958uac00 ubc1cuc0ddud588uc2b5ub2c8ub2e4?</span>
                            <button
                              onClick={() => handleDelete(story.id)}
                              className="text-red-600 hover:text-red-900 dark:text-red-400 dark:hover:text-red-300"
                            >
                              uc5c5ub370
                            </button>
                            <button
                              onClick={cancelDelete}
                              className="text-gray-600 hover:text-gray-900 dark:text-gray-400 dark:hover:text-gray-300"
                            >
                              ucde8uc18c
                            </button>
                          </div>
                        ) : (
                          <div className="flex items-center justify-end space-x-3">
                            <Link
                              to={`/admin/story/${story.id}`}
                              className="text-indigo-600 hover:text-indigo-900 dark:text-indigo-400 dark:hover:text-indigo-300"
                            >
                              <FaEdit className="inline" />
                            </Link>
                            <button
                              onClick={() => confirmDelete(story.id)}
                              className="text-red-600 hover:text-red-900 dark:text-red-400 dark:hover:text-red-300"
                            >
                              <FaTrash className="inline" />
                            </button>
                          </div>
                        )}
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          )}
        </motion.div>
      </div>
    </div>
  );
};

export default Dashboard;
